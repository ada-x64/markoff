<template>
    <property name="value" />
    <button
        width="100%"
        hover:background="#111"
        padding="5px"
        tag:value="{value}"
        border="0 0 1px 0"
        border_color="#fffa"
        zindex="100"
    >
        <text font_size="12px">{value}</text>
    </button>
</template>
