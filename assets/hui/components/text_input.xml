<template>
<property name="on_spawn" />
<property name="name" />
<property name="default_text" />
    <button
        on_spawn="{on_spawn}"
        target="text_value"
        tag:filter="text"
        background="#222"
        border="2px"
        min_height="40px"
        border_color="#555"
        padding="5px"
        active:border_color="#FFF"
    >
        <text tag:name="{name}" font_size="20">{default_text}</text>
    </button>
</template>
